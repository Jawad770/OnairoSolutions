# Demos moved

Production showcase demos live only in:

`public/demos/`

Served as `/showcase/<name>` (for example `/showcase/carshowroom`).

Do not add HTML here — `npm run build:public` no longer copies this folder.
